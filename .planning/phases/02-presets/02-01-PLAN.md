---
phase: 02-presets
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - creator/src/lib/tuning/presets.ts
  - creator/src/lib/tuning/__tests__/presets.test.ts
autonomous: true
requirements:
  - PRES-01
  - PRES-02
  - PRES-03
must_haves:
  truths:
    - "Three preset constants (CASUAL_PRESET, BALANCED_PRESET, HARDCORE_PRESET) are exported and importable"
    - "Each preset covers all 137 tunable fields from FIELD_METADATA with no undefined values"
    - "Presets are typed as DeepPartial<AppConfig> overlays"
    - "Each preset has metadata: id, name, description, sectionDescriptions"
  artifacts:
    - path: "creator/src/lib/tuning/presets.ts"
      provides: "TuningPreset interface + 3 preset constants + TUNING_PRESETS array"
      exports: ["TuningPreset", "CASUAL_PRESET", "BALANCED_PRESET", "HARDCORE_PRESET", "TUNING_PRESETS"]
    - path: "creator/src/lib/tuning/__tests__/presets.test.ts"
      provides: "Coverage tests and structural validation"
      contains: "FIELD_METADATA"
  key_links:
    - from: "creator/src/lib/tuning/presets.ts"
      to: "creator/src/lib/tuning/types.ts"
      via: "imports DeepPartial, TuningSection"
      pattern: "import.*DeepPartial.*from.*types"
    - from: "creator/src/lib/tuning/presets.ts"
      to: "creator/src/types/config.ts"
      via: "imports AppConfig for type annotation"
      pattern: "import.*AppConfig.*from.*config"
---

<objective>
Define the TuningPreset interface and author all three themed presets (Casual, Balanced, Hardcore) as DeepPartial<AppConfig> overlays covering all 137 tunable fields, with TDD coverage tests.

Purpose: Presets are the core data artifact of Phase 2. Downstream phases (wizard workspace, comparison view) consume these directly.
Output: `presets.ts` with interface + 3 constants, `presets.test.ts` with coverage validation.
</objective>

<execution_context>
@C:/Users/John Noecker Jr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/John Noecker Jr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-presets/02-CONTEXT.md
@.planning/phases/02-presets/02-RESEARCH.md

@creator/src/lib/tuning/types.ts
@creator/src/lib/tuning/fieldMetadata.ts
@creator/src/lib/tuning/formulas.ts
@creator/src/lib/tuning/diffEngine.ts
@creator/src/lib/tuning/__tests__/formulas.test.ts
@creator/src/types/config.ts
@creator/src/lib/templates.ts

<interfaces>
<!-- Key types the executor needs from Phase 1 artifacts -->

From creator/src/lib/tuning/types.ts:
```typescript
export type DeepPartial<T> = {
  [P in keyof T]?: T[P] extends object ? DeepPartial<T[P]> : T[P];
};

export const enum TuningSection {
  CombatStats = "Combat & Stats",
  EconomyCrafting = "Economy & Crafting",
  ProgressionQuests = "Progression & Quests",
  WorldSocial = "World & Social",
}
```

From creator/src/lib/tuning/fieldMetadata.ts:
```typescript
export const FIELD_METADATA: Record<string, FieldMeta> = { /* 137 entries */ };
```

From creator/src/lib/tuning/diffEngine.ts:
```typescript
function getNestedValue(obj: Record<string, unknown>, path: string): unknown;
```

From creator/src/lib/templates.ts:
```typescript
export function applyTemplate(config: AppConfig, overrides: DeepPartial<AppConfig>): AppConfig;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TuningPreset interface + RED coverage tests</name>
  <files>creator/src/lib/tuning/presets.ts, creator/src/lib/tuning/__tests__/presets.test.ts</files>
  <read_first>
    - creator/src/lib/tuning/types.ts (DeepPartial, TuningSection types)
    - creator/src/lib/tuning/fieldMetadata.ts (all 137 field paths -- the source of truth for coverage)
    - creator/src/types/config.ts (AppConfig interface -- the shape presets overlay)
    - creator/src/lib/tuning/__tests__/formulas.test.ts (test patterns, mockConfig shape)
    - creator/src/lib/templates.ts (applyTemplate function, existing DeepPartial usage)
  </read_first>
  <behavior>
    - Test: TUNING_PRESETS array has exactly 3 entries
    - Test: Each preset has id matching "casual" | "balanced" | "hardcore"
    - Test: Each preset has non-empty name, description strings
    - Test: Each preset has sectionDescriptions with entries for all 4 TuningSection values
    - Test: For each of the 137 paths in Object.keys(FIELD_METADATA), getNestedValue(preset.config, path) !== undefined
    - Test: TypeScript compiles without error (preset.config typed as DeepPartial<AppConfig>)
  </behavior>
  <action>
    1. Create `creator/src/lib/tuning/presets.ts` with a stub:
       - Define `TuningPreset` interface with fields: `id: string`, `name: string`, `description: string`, `sectionDescriptions: Partial<Record<TuningSection, string>>`, `config: DeepPartial<AppConfig>`
       - Export empty stub constants: `CASUAL_PRESET`, `BALANCED_PRESET`, `HARDCORE_PRESET` with `config: {}` (will fail coverage tests)
       - Export `TUNING_PRESETS` array containing all three

    2. Create `creator/src/lib/tuning/__tests__/presets.test.ts` with:
       - Import `FIELD_METADATA` from `@/lib/tuning/fieldMetadata`
       - Import `TUNING_PRESETS` from `@/lib/tuning/presets`
       - Implement `getNestedValue()` helper (copy pattern from diffEngine.ts):
         ```typescript
         function getNestedValue(obj: Record<string, unknown>, path: string): unknown {
           return path.split(".").reduce<unknown>(
             (acc, key) => (acc && typeof acc === "object" ? (acc as Record<string, unknown>)[key] : undefined),
             obj,
           );
         }
         ```
       - "preset structure" describe block: assert TUNING_PRESETS.length === 3, each has id in ["casual", "balanced", "hardcore"], each has non-empty name and description
       - "sectionDescriptions" describe block: for each preset, assert sectionDescriptions has keys for "Combat & Stats", "Economy & Crafting", "Progression & Quests", "World & Social"
       - "field coverage" describe block: for each preset, iterate all `Object.keys(FIELD_METADATA)` and assert `getNestedValue(preset.config as Record<string, unknown>, path)` is not undefined, with error message `${preset.id} missing field: ${path}`

    3. Run tests -- they MUST fail (RED) because preset configs are empty stubs.

    Per D-01: coverage tests enforce that every field in FIELD_METADATA is present.
    Per PRES-03: TuningPreset.config is typed as DeepPartial<AppConfig>.
  </action>
  <verify>
    <automated>cd C:/claudework/Arcanum/fervent-archimedes/creator && bun run test -- --run src/lib/tuning/__tests__/presets.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - creator/src/lib/tuning/presets.ts exists and contains `export interface TuningPreset`
    - creator/src/lib/tuning/presets.ts contains `export const CASUAL_PRESET: TuningPreset`
    - creator/src/lib/tuning/presets.ts contains `export const BALANCED_PRESET: TuningPreset`
    - creator/src/lib/tuning/presets.ts contains `export const HARDCORE_PRESET: TuningPreset`
    - creator/src/lib/tuning/presets.ts contains `export const TUNING_PRESETS`
    - creator/src/lib/tuning/__tests__/presets.test.ts exists and contains `FIELD_METADATA`
    - creator/src/lib/tuning/__tests__/presets.test.ts contains `getNestedValue`
    - Tests run but FAIL (RED phase) because preset configs are stubs
    - `bunx tsc --noEmit` passes (types are correct even with empty config)
  </acceptance_criteria>
  <done>TuningPreset interface exported, 3 stub constants exported, test file runs and fails on coverage checks (RED)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author all 3 preset configs to pass coverage tests (GREEN)</name>
  <files>creator/src/lib/tuning/presets.ts</files>
  <read_first>
    - creator/src/lib/tuning/presets.ts (current stub state from Task 1)
    - creator/src/lib/tuning/fieldMetadata.ts (ALL 137 field paths -- read the ENTIRE file to know every path)
    - creator/src/types/config.ts (AppConfig interface -- need exact field names and nesting)
    - creator/src/lib/tuning/formulas.ts (computeMetrics -- understand what formulas evaluate so values make sense)
    - reference/config/AppConfig.kt (Kotlin defaults for authoring context -- NOT as baseline per D-03)
    - creator/src/lib/tuning/__tests__/presets.test.ts (understand what tests expect)
  </read_first>
  <behavior>
    - All coverage tests pass: every FIELD_METADATA path resolves to a defined value in each preset
    - Structure tests pass: 3 presets, correct ids, non-empty metadata
    - sectionDescriptions tests pass: all 4 sections have descriptions
    - TypeScript compiles: `bunx tsc --noEmit` exits 0
  </behavior>
  <action>
    Fill in the `config` property of all three preset constants in `presets.ts`. The config must be a `DeepPartial<AppConfig>` covering all 137 tunable scalar fields from FIELD_METADATA.

    **Structure:** Organize each preset's config with section comment dividers matching FIELD_METADATA sections:
    ```
    // --- Combat ---
    // --- Mob Tiers ---
    // --- Stats Bindings ---
    // --- Economy ---
    // ... etc.
    ```

    **Preset philosophy (per D-04, Claude's discretion on exact values):**

    CASUAL_PRESET (id: "casual"):
    - name: "Casual Adventure"
    - description: "A relaxed experience with faster progression, gentler combat, and generous rewards. Ideal for story-focused or solo play."
    - XP: lower exponent (~1.6), baseXp ~80 -- much faster leveling
    - Mob tiers: 30-50% lower HP and damage vs Balanced
    - Economy: higher sell multiplier (~0.6), lower buy multiplier (~0.8) -- generous gold
    - Regen: faster base interval (~3500ms) -- less downtime
    - Stats: lower divisors (more impactful stats)
    - All convenience systems enabled (housing, gambling, lottery, dailyQuests, autoQuests)

    BALANCED_PRESET (id: "balanced"):
    - name: "Balanced Realm"
    - description: "A well-tuned middle ground with satisfying combat pacing, steady progression, and a healthy economy. Good for most builders."
    - XP: moderate exponent (~1.8), baseXp ~100 -- steady but not grindy
    - Mob tiers: tuned for 3-5 hit kills on standard mobs at level
    - Economy: balanced buy/sell spread (buy ~1.0, sell ~0.5)
    - Regen: moderate interval (~4500ms)
    - All systems enabled with moderate values

    HARDCORE_PRESET (id: "hardcore"):
    - name: "Hardcore Challenge"
    - description: "A punishing world where every level is earned, resources are scarce, and combat demands preparation. For experienced MUD players."
    - XP: steep exponent (~2.2), baseXp ~120 -- significant grind at high levels
    - Mob tiers: 50-100% higher HP and damage vs Balanced
    - Economy: tight (buy ~1.3, sell ~0.35) -- scarce gold
    - Regen: slower interval (~6000ms) -- forces potion/healer dependency
    - Prestige: higher costs, more ranks for extended endgame

    **Per D-02:** Low-impact fields (weather timing, navigation cooldowns, friend limits) MAY share the same value across presets. Only fields that shape gameplay feel need to differ.

    **Per D-03:** Balanced is NOT identical to Kotlin defaults. It's a distinct opinionated config.

    **Per D-05:** No DEFAULT_CONFIG constant. Presets are standalone overlays.

    Also fill in `sectionDescriptions` for each preset with 1-2 sentence philosophy per TuningSection:
    - CombatStats: describe combat feel
    - EconomyCrafting: describe economic philosophy
    - ProgressionQuests: describe leveling pace and quest approach
    - WorldSocial: describe world pacing and social systems

    After filling all values, run the test suite. All coverage tests must pass (GREEN).
  </action>
  <verify>
    <automated>cd C:/claudework/Arcanum/fervent-archimedes/creator && bun run test -- --run src/lib/tuning/__tests__/presets.test.ts && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All tests in presets.test.ts pass (exit code 0)
    - `bunx tsc --noEmit` exits 0
    - creator/src/lib/tuning/presets.ts contains values for all 137 FIELD_METADATA paths in each preset (verified by passing coverage tests)
    - CASUAL_PRESET.config contains `progression` with `xp` section having exponent < 1.8
    - HARDCORE_PRESET.config contains `progression` with `xp` section having exponent > 2.0
    - Each preset's sectionDescriptions has 4 entries (one per TuningSection)
    - No field in any preset has value `undefined` or `null`
  </acceptance_criteria>
  <done>All 3 presets fully authored with 137 fields each, all coverage tests pass (GREEN), TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
1. `cd creator && bun run test -- --run src/lib/tuning/__tests__/presets.test.ts` -- all tests pass
2. `cd creator && bunx tsc --noEmit` -- no type errors
3. `TUNING_PRESETS.length === 3` verified by test
4. Each preset covers all 137 FIELD_METADATA paths verified by coverage test
</verification>

<success_criteria>
- TuningPreset interface exported from presets.ts
- 3 preset constants (CASUAL, BALANCED, HARDCORE) exported with full config overlays
- TUNING_PRESETS array exported for iteration
- All 137 tunable fields defined in every preset (no undefined fallthrough per D-01)
- TypeScript compiles without errors (PRES-03)
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/02-presets/02-01-SUMMARY.md`
</output>
