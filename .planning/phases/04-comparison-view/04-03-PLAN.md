---
phase: 04-comparison-view
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - creator/src/components/tuning/ParameterRow.tsx
  - creator/src/components/tuning/ParameterSection.tsx
  - creator/src/index.css
autonomous: false
requirements:
  - COMP-05
  - UI-04
must_haves:
  truths:
    - "Every parameter row label has a hover tooltip showing description, optional interaction note, and impact badge"
    - "Changed parameter values show arrow + percentage delta alongside the preset value"
    - "Increases are green (text-status-success), decreases are muted red (text-status-error), unchanged are muted (text-text-muted)"
    - "Collapsed section headers show a changes-count badge when a preset is active"
    - "Tippy tooltips use the arcanum dark theme, not the default light theme"
  artifacts:
    - path: "creator/src/components/tuning/ParameterRow.tsx"
      provides: "Enhanced parameter row with tooltips and delta badges"
      contains: "tippy"
    - path: "creator/src/components/tuning/ParameterSection.tsx"
      provides: "Changes-count badge on collapsed headers"
      contains: "changed"
    - path: "creator/src/index.css"
      provides: "Arcanum Tippy theme CSS and tippy.css import"
      contains: "data-theme"
  key_links:
    - from: "creator/src/components/tuning/ParameterRow.tsx"
      to: "creator/src/lib/tuning/deltaUtils.ts"
      via: "imports pctDelta, deltaDirection, deltaColor, buildTooltipContent"
      pattern: "import.*from.*deltaUtils"
    - from: "creator/src/components/tuning/ParameterRow.tsx"
      to: "tippy.js"
      via: "imports tippy for tooltip creation"
      pattern: "import tippy from.*tippy.js"
    - from: "creator/src/index.css"
      to: "tippy.js/dist/tippy.css"
      via: "CSS import for base Tippy styles"
      pattern: "tippy.js/dist/tippy.css"
---

<objective>
Enhance ParameterRow with Tippy tooltips (UI-04) and arrow + percentage delta display (COMP-05). Update diff colors from Phase 3's warning/info to success/error (D-08). Add changes-count badge to ParameterSection. Add Tippy arcanum theme CSS to index.css.

Purpose: Every field in the comparison gets contextual help and magnitude indicators. Builders can hover any label to understand what the field does, and see at-a-glance how much each value changed.
Output: Enhanced ParameterRow, enhanced ParameterSection, Tippy theme CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-comparison-view/04-CONTEXT.md
@.planning/phases/04-comparison-view/04-RESEARCH.md
@.planning/phases/04-comparison-view/04-UI-SPEC.md
@.planning/phases/04-comparison-view/04-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From creator/src/lib/tuning/deltaUtils.ts (created in Plan 01):
```typescript
export type DeltaDirection = "up" | "down" | "same";
export function deltaDirection(oldVal: number, newVal: number): DeltaDirection;
export function deltaColor(direction: DeltaDirection): string;
export function pctDelta(oldVal: number, newVal: number): string;
export function buildTooltipContent(meta: FieldMeta): string;
```

From creator/src/lib/tuning/types.ts:
```typescript
export interface FieldMeta {
  label: string;
  description: string;
  section: TuningSection;
  min?: number;
  max?: number;
  impact: "high" | "medium" | "low";
  interactionNote?: string;
}

export interface DiffEntry {
  path: string;
  label: string;
  section: TuningSection;
  oldValue: unknown;
  newValue: unknown;
}
```

From creator/src/components/tuning/ParameterRow.tsx (current state):
```typescript
interface ParameterRowProps {
  path: string;
  meta: FieldMeta;
  currentValue: unknown;
  presetValue?: unknown;
  isChanged: boolean;
  hasPreset: boolean;
  presetAccentBorder?: string;
  even: boolean;
}

// Current diffColor uses text-status-warning for increase, text-status-info for decrease
// These must change to text-status-success and text-status-error per D-08
```

From creator/src/components/tuning/ParameterSection.tsx (current state):
```typescript
interface ParameterSectionProps {
  section: TuningSection;
  fields: [string, FieldMeta][];
  currentConfig: Record<string, unknown>;
  diffMap: Map<string, DiffEntry>;
  hasPreset: boolean;
  isCollapsed: boolean;
  onToggleCollapsed: () => void;
  presetAccentBorder?: string;
}
```

Tippy.js usage pattern from creator/src/components/lore/LoreEditor.tsx:
```typescript
import tippy, { type Instance as TippyInstance } from "tippy.js";
// Used imperatively via tippy("body", { ... }) in useEffect
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Tippy CSS import and arcanum theme to index.css, then enhance ParameterRow with tooltips and delta display</name>
  <files>creator/src/index.css, creator/src/components/tuning/ParameterRow.tsx</files>
  <read_first>
    - creator/src/index.css (existing @import statements at top, :root and @theme blocks, existing CSS custom properties)
    - creator/src/components/tuning/ParameterRow.tsx (current full content -- ParameterRowProps, formatValue, diffColor, JSX)
    - creator/src/lib/tuning/deltaUtils.ts (pctDelta, deltaDirection, deltaColor, buildTooltipContent exports)
    - creator/src/lib/tuning/types.ts (FieldMeta interface)
    - creator/src/components/lore/LoreEditor.tsx (lines 1-15 and 185-200 for Tippy usage pattern)
    - .planning/phases/04-comparison-view/04-UI-SPEC.md (ParameterRow changes, tooltip design, color roles, tippy theme CSS)
  </read_first>
  <action>
**Step 1: Add tippy.css import and arcanum theme to index.css**

At the top of `creator/src/index.css`, after the existing `@import "tailwindcss"` line, add:
```css
@import "tippy.js/dist/tippy.css";
```

Then at the END of `creator/src/index.css` (after all existing rules), add the Arcanum Tippy theme:
```css
/* ─── Arcanum Tippy Theme ──────────────────────────────────────────── */
[data-theme~='arcanum'] .tippy-box {
  background-color: var(--color-bg-elevated);
  border: 1px solid var(--color-border-default);
  color: var(--color-text-primary);
  font-family: var(--font-sans);
  font-size: 13px;
}
[data-theme~='arcanum'] .tippy-box[data-placement^='top'] > .tippy-arrow::before {
  border-top-color: var(--color-bg-elevated);
}
[data-theme~='arcanum'] .tippy-box[data-placement^='bottom'] > .tippy-arrow::before {
  border-bottom-color: var(--color-bg-elevated);
}
[data-theme~='arcanum'] .tippy-box[data-placement^='left'] > .tippy-arrow::before {
  border-left-color: var(--color-bg-elevated);
}
[data-theme~='arcanum'] .tippy-box[data-placement^='right'] > .tippy-arrow::before {
  border-right-color: var(--color-bg-elevated);
}
```

**Step 2: Rewrite ParameterRow.tsx with tooltips and delta display**

Replace the entire `creator/src/components/tuning/ParameterRow.tsx` with an enhanced version. Key changes:

1. **Add imports:**
```typescript
import { useRef, useEffect } from "react";
import tippy from "tippy.js";
import type { FieldMeta } from "@/lib/tuning/types";
import { pctDelta, deltaDirection, deltaColor, buildTooltipContent } from "@/lib/tuning/deltaUtils";
```

2. **Replace `diffColor` function** with the new utilities from deltaUtils:
   - Remove the old `diffColor` function entirely (it used text-status-warning and text-status-info)
   - Use `deltaDirection(currentValue, presetValue)` + `deltaColor(direction)` instead
   - Per D-08: increase = `text-status-success`, decrease = `text-status-error`, unchanged = `text-text-muted`

3. **Add Tippy tooltip on label span (D-10, D-11):**
   - Add `useRef<HTMLSpanElement>(null)` for the label element
   - In a `useEffect`, attach a tippy instance:
     ```typescript
     useEffect(() => {
       if (!labelRef.current) return;
       const content = buildTooltipContent(meta);
       const instance = tippy(labelRef.current, {
         content,
         allowHTML: true,
         theme: "arcanum",
         placement: "top-start",
         delay: [250, 100],
         maxWidth: 280,
       });
       return () => instance.destroy();
     }, [meta.description, meta.interactionNote, meta.impact]);
     ```
   - Add `ref={labelRef}` and `className="cursor-default"` to the label `<span>`

4. **Add arrow + percentage delta alongside preset value (D-09):**
   - When `isChanged` is true and both values are numeric: compute `pctDelta(currentValue as number, presetValue as number)` and display it next to the formatted preset value
   - When `isChanged` is true but values are non-numeric: show preset value in `text-status-success` with no percentage
   - When `isChanged` is false: show preset value in `text-text-muted`
   - Format: `{formattedPresetValue} {deltaString}` where deltaString is the output of pctDelta, all in a single `<span>`

5. **Update grid layout** for the with-preset case to accommodate the wider delta column:
   - No-preset: `grid-cols-[1.2fr_100px_1.5fr]` (unchanged)
   - With-preset: `grid-cols-[1.2fr_80px_140px_1.5fr]` (narrow current column, wider preset+delta column)

6. **Keep existing functionality:**
   - `formatValue` function unchanged
   - `even` prop striping unchanged
   - `presetAccentBorder` left-border highlighting unchanged
   - Hover background unchanged

**Full revised component structure:**

```typescript
// --- Parameter Row ----------------------------------------------------------
// Single parameter row showing label with tooltip, current value, optional
// preset value with diff highlighting and percentage delta.

import { useRef, useEffect } from "react";
import tippy from "tippy.js";
import type { FieldMeta } from "@/lib/tuning/types";
import { pctDelta, deltaDirection, deltaColor, buildTooltipContent } from "@/lib/tuning/deltaUtils";

interface ParameterRowProps {
  path: string;
  meta: FieldMeta;
  currentValue: unknown;
  presetValue?: unknown;
  isChanged: boolean;
  hasPreset: boolean;
  presetAccentBorder?: string;
  even: boolean;
}

function formatValue(v: unknown): string {
  if (v === undefined || v === null) return "\u2014";
  if (typeof v === "boolean") return v ? "true" : "false";
  if (typeof v === "number") return String(v);
  return String(v);
}

export function ParameterRow({
  meta,
  currentValue,
  presetValue,
  isChanged,
  hasPreset,
  presetAccentBorder,
  even,
}: ParameterRowProps) {
  const labelRef = useRef<HTMLSpanElement>(null);

  // Tooltip on field label (D-10, D-11, UI-04)
  useEffect(() => {
    if (!labelRef.current) return;
    const content = buildTooltipContent(meta);
    const instance = tippy(labelRef.current, {
      content,
      allowHTML: true,
      theme: "arcanum",
      placement: "top-start",
      delay: [250, 100],
      maxWidth: 280,
    });
    return () => instance.destroy();
  }, [meta.description, meta.interactionNote, meta.impact]);

  const gridCols = hasPreset
    ? "grid-cols-[1.2fr_80px_140px_1.5fr]"
    : "grid-cols-[1.2fr_100px_1.5fr]";

  const rowHighlight =
    isChanged && presetAccentBorder ? `border-l-2 ${presetAccentBorder}` : "";

  const stripe = even ? "bg-bg-secondary/50" : "";

  // Determine preset cell content and color (D-08, D-09)
  let presetDisplay = "";
  let presetColorClass = "text-text-muted";
  if (hasPreset) {
    const formatted = formatValue(presetValue);
    if (!isChanged) {
      presetDisplay = formatted;
      presetColorClass = "text-text-muted";
    } else if (typeof currentValue === "number" && typeof presetValue === "number") {
      const dir = deltaDirection(currentValue, presetValue);
      presetColorClass = deltaColor(dir);
      const delta = pctDelta(currentValue, presetValue);
      presetDisplay = dir === "same" ? `${formatted} \u2014` : `${formatted} ${delta}`;
    } else {
      // Non-numeric but changed
      presetDisplay = formatted;
      presetColorClass = "text-status-success";
    }
  }

  return (
    <div
      className={`grid min-h-[40px] items-center gap-x-4 py-2 px-2 transition-colors duration-200 hover:bg-bg-hover ${gridCols} ${rowHighlight} ${stripe}`}
    >
      {/* Label with tooltip */}
      <span
        ref={labelRef}
        className="cursor-default font-sans text-[15px] font-semibold text-text-primary"
      >
        {meta.label}
      </span>

      {/* Current value */}
      <span className="text-right font-mono text-sm text-text-secondary">
        {formatValue(currentValue)}
      </span>

      {/* Preset value + delta (only when a preset is selected) */}
      {hasPreset && (
        <span className={`text-right font-mono text-sm ${presetColorClass}`}>
          {presetDisplay}
        </span>
      )}

      {/* Description */}
      <span className="truncate font-sans text-sm text-text-muted">
        {meta.description}
      </span>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>cd /c/claudework/Arcanum/fervent-archimedes/creator && bunx tsc --noEmit && bun run test</automated>
  </verify>
  <acceptance_criteria>
    - creator/src/index.css contains `@import "tippy.js/dist/tippy.css"`
    - creator/src/index.css contains `[data-theme~='arcanum'] .tippy-box`
    - creator/src/index.css contains `border-top-color: var(--color-bg-elevated)`
    - creator/src/index.css contains `border-bottom-color: var(--color-bg-elevated)`
    - creator/src/components/tuning/ParameterRow.tsx contains `import tippy from "tippy.js"`
    - creator/src/components/tuning/ParameterRow.tsx contains `import.*buildTooltipContent.*deltaUtils`
    - creator/src/components/tuning/ParameterRow.tsx contains `import.*pctDelta.*deltaUtils`
    - creator/src/components/tuning/ParameterRow.tsx contains `theme: "arcanum"`
    - creator/src/components/tuning/ParameterRow.tsx contains `allowHTML: true`
    - creator/src/components/tuning/ParameterRow.tsx does NOT contain `text-status-warning` (old Phase 3 color removed)
    - creator/src/components/tuning/ParameterRow.tsx does NOT contain `text-status-info` (old Phase 3 color removed)
    - creator/src/components/tuning/ParameterRow.tsx contains `grid-cols-[1.2fr_80px_140px_1.5fr]`
    - `bunx tsc --noEmit` exits 0
    - `bun run test` exits 0
  </acceptance_criteria>
  <done>ParameterRow has Tippy tooltips on all field labels (UI-04) with description + interactionNote + impact badge. Preset values show arrow + percentage delta (D-09). Colors corrected to success/error (D-08). Tippy arcanum theme CSS added to index.css. Full test suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add changes-count badge to ParameterSection collapsed headers</name>
  <files>creator/src/components/tuning/ParameterSection.tsx</files>
  <read_first>
    - creator/src/components/tuning/ParameterSection.tsx (current full content -- ParameterSectionProps, header JSX, badge rendering)
    - creator/src/lib/tuning/types.ts (DiffEntry interface)
    - .planning/phases/04-comparison-view/04-UI-SPEC.md (changes-count badge spec, copy contract)
    - .planning/phases/04-comparison-view/04-RESEARCH.md (Pattern 6: Changes-Count Badge)
  </read_first>
  <action>
Enhance `creator/src/components/tuning/ParameterSection.tsx` to show a "{N} changed" badge on collapsed section headers when a preset is active.

**Changes:**

1. Add `useMemo` import from React:
```typescript
import { useMemo } from "react";
```

2. Inside the `ParameterSection` component, compute `changedCount`:
```typescript
const changedCount = useMemo(
  () => fields.filter(([path]) => diffMap.has(path)).length,
  [fields, diffMap],
);
```

3. In the header `<button>` JSX, after the existing field-count badge `<span>`, add the changes badge:
```tsx
{/* Changes count badge -- shown when collapsed with preset active */}
{hasPreset && changedCount > 0 && (
  <span className="rounded-full bg-status-success/[0.14] px-2 py-0.5 font-sans text-[13px] text-status-success">
    {changedCount} changed
  </span>
)}
```

This badge appears inline after the existing field count badge (e.g., "Combat & Stats [34] [12 changed]"). It uses the same rounded-full pill pattern as the existing badge but with success green color instead of accent purple.

**No other changes to ParameterSection.** The collapse/expand behavior, field rendering, and getNestedValue helper remain unchanged.
  </action>
  <verify>
    <automated>cd /c/claudework/Arcanum/fervent-archimedes/creator && bunx tsc --noEmit && bun run test</automated>
  </verify>
  <acceptance_criteria>
    - creator/src/components/tuning/ParameterSection.tsx contains `import { useMemo } from "react"`
    - creator/src/components/tuning/ParameterSection.tsx contains `changedCount`
    - creator/src/components/tuning/ParameterSection.tsx contains `changed`  (the word "changed" in JSX)
    - creator/src/components/tuning/ParameterSection.tsx contains `bg-status-success/[0.14]`
    - creator/src/components/tuning/ParameterSection.tsx contains `text-status-success`
    - creator/src/components/tuning/ParameterSection.tsx contains `diffMap.has(path)`
    - `bunx tsc --noEmit` exits 0
    - `bun run test` exits 0
  </acceptance_criteria>
  <done>Collapsed section headers show "{N} changed" badge in success green when a preset is active and at least 1 field has changed. Badge disappears when section is expanded or no preset is selected. Full test suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of comparison view</name>
  <files>creator/src/components/tuning/TuningWizard.tsx</files>
  <action>
This is a visual verification checkpoint. No code changes are needed -- the executor should launch the app and guide the user through verification of all Phase 4 features built across Plans 02 and 03.

Run `cd /c/claudework/Arcanum/fervent-archimedes/creator && bun run tauri dev` and present the verification steps below to the user.
  </action>
  <verify>
    <automated>cd /c/claudework/Arcanum/fervent-archimedes/creator && bunx tsc --noEmit && bun run test</automated>
  </verify>
  <what-built>
Complete Phase 4 comparison view: metric summary cards (2x2 grid with derived metrics at Lv10/30/50), Tippy tooltips on all parameter labels, arrow + percentage deltas on changed values, corrected green/red diff colors, and changes-count badges on collapsed section headers.
  </what-built>
  <how-to-verify>
1. Run `cd /c/claudework/Arcanum/fervent-archimedes/creator && bun run tauri dev`
2. Open a project with an application.yaml
3. Click "Tuning Wizard" in the sidebar

**Before selecting a preset:**
- Verify NO metric cards are visible (just preset cards + parameter browser)
- Hover over any parameter label -- verify a dark-themed tooltip appears with description text and an impact badge (HIGH/MEDIUM/LOW)

**Select the "Casual" preset card:**
- Verify a 2x2 grid of metric cards appears between preset cards and the search bar
- Verify the 4 cards are labeled: COMBAT & STATS, PROGRESSION & QUESTS, ECONOMY & CRAFTING, WORLD & SOCIAL
- Verify each card shows 2-3 metric rows with "Current" and "Preset" column headers
- Verify delta values show arrows (up/down) with percentages in green/red colors
- Verify all parameter sections below collapsed automatically
- Verify collapsed section headers show "{N} changed" badges in green

**Verify metric card content:**
- Combat & Stats card should show Mob HP at Lv10 and Lv30, plus Dodge Chance
- Progression & Quests should show XP to Lv10, XP to Lv30, Player HP Lv10
- Economy & Crafting should show Gold/Kill at Lv10 and Lv30, with footnote about buy/sell multipliers
- World & Social should show Regen Interval with a changed-count footnote

**Verify tooltips on metric card rows:**
- Hover over a metric label like "Mob HP -- Lv10" -- verify a tooltip appears with the formula explanation

**Verify parameter row diff display:**
- Expand any section
- Check that changed values show the preset value followed by an arrow and a percentage
- Check that increases are green, decreases are muted red, unchanged values are muted gray

**Select "Hardcore" preset:**
- Verify metric cards update with new values (no page refresh)
- Verify all sections collapse again
- Verify delta arrows and percentages reflect the new preset

**Deselect the preset (click the selected card again):**
- Verify metric cards disappear
- Verify sections remain in their current collapsed/expanded state (not reset)
  </how-to-verify>
  <done>User has visually verified all Phase 4 features: metric cards, tooltips, delta display, color coding, collapse behavior, and changes-count badges.</done>
  <resume-signal>Type "approved" to proceed, or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tippy HTML content | buildTooltipContent generates HTML from internal FieldMeta; allowHTML: true is used |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | S (Spoofing) | Tippy tooltip HTML via allowHTML | accept | All tooltip content comes from hardcoded FIELD_METADATA in fieldMetadata.ts (137 entries authored by developers). No user-provided strings enter buildTooltipContent. No XSS vector exists. |
| T-04-05 | D (Denial of Service) | Tippy instance creation per ParameterRow | accept | Each row creates one tippy instance, destroyed on unmount. 137 max rows. Tippy is designed for this volume. |
</threat_model>

<verification>
- `bunx tsc --noEmit` passes
- `bun run test` full suite green
- Tippy tooltips render with dark theme (not default light)
- ParameterRow shows arrow + percentage delta for numeric changes
- ParameterRow uses text-status-success/error (not warning/info)
- ParameterSection shows changes-count badge when collapsed with preset
- Visual verification confirms all behaviors in checkpoint task
</verification>

<success_criteria>
- Tippy CSS and arcanum theme in index.css
- Every parameter label has a hover tooltip with description, interactionNote, impact badge
- Changed values display arrow + percentage delta
- Green for increase, red for decrease, muted for unchanged (D-08 corrected from Phase 3)
- Collapsed section headers show "{N} changed" badge
- Human visual verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/04-comparison-view/04-03-SUMMARY.md`
</output>
