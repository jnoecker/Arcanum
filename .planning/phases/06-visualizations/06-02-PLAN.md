---
phase: 06-visualizations
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - creator/src/components/tuning/charts/ChartRow.tsx
  - creator/src/components/tuning/charts/XpCurveChart.tsx
  - creator/src/components/tuning/charts/MobTierChart.tsx
  - creator/src/components/tuning/charts/StatRadarChart.tsx
  - creator/src/components/tuning/TuningWizard.tsx
autonomous: false
requirements:
  - VIZ-01
  - VIZ-02
  - VIZ-03
must_haves:
  truths:
    - "An XP curve line chart shows XP-per-level for both current config and selected preset across levels 1-50"
    - "A mob tier bar chart shows HP, damage, armor, and XP across all 4 tiers at a user-selectable level (Lv10, Lv20, Lv30, Lv50)"
    - "A stat profile radar chart compares current vs preset stat scaling effectiveness with 8 stat dimensions"
    - "Charts appear only when a preset is selected and disappear when deselected"
    - "Charts sit between MetricSectionCards and HealthCheckBanner in the wizard layout"
    - "Three chart cards are side by side in a single row (grid-cols-3)"
    - "Preset data series uses warm aurum-gold color; current uses muted secondary tone"
    - "Chart animations transition smoothly over 300ms ease-out when preset changes"
  artifacts:
    - path: "creator/src/components/tuning/charts/ChartRow.tsx"
      provides: "Container with 3-column grid layout for chart cards"
      exports: ["ChartRow"]
    - path: "creator/src/components/tuning/charts/XpCurveChart.tsx"
      provides: "Recharts LineChart for XP curve comparison"
      exports: ["XpCurveChart"]
    - path: "creator/src/components/tuning/charts/MobTierChart.tsx"
      provides: "Recharts BarChart for mob tier power at selectable level"
      exports: ["MobTierChart"]
    - path: "creator/src/components/tuning/charts/StatRadarChart.tsx"
      provides: "Recharts RadarChart for stat profile comparison"
      exports: ["StatRadarChart"]
    - path: "creator/src/components/tuning/TuningWizard.tsx"
      provides: "Updated wizard layout with ChartRow inserted"
      contains: "ChartRow"
  key_links:
    - from: "creator/src/components/tuning/charts/ChartRow.tsx"
      to: "creator/src/lib/tuning/chartData.ts"
      via: "imports buildXpCurveData, buildMobTierData, buildStatRadarData"
      pattern: "import.*buildXpCurveData.*from.*chartData"
    - from: "creator/src/components/tuning/charts/XpCurveChart.tsx"
      to: "recharts"
      via: "imports LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer"
      pattern: "import.*LineChart.*from.*recharts"
    - from: "creator/src/components/tuning/TuningWizard.tsx"
      to: "creator/src/components/tuning/charts/ChartRow.tsx"
      via: "imports and renders ChartRow conditionally"
      pattern: "import.*ChartRow.*from.*charts/ChartRow"
---

<objective>
Build the three Recharts chart components (XP curve, mob tier, stat radar) inside a ChartRow container, then wire ChartRow into TuningWizard.tsx between the metric cards and health check banner.

Purpose: Deliver the visual chart experience that makes formula interactions intuitive for MUD builders -- the core value of Phase 6. Charts consume the pure data functions from Plan 01.

Output: 4 new component files in charts/ directory, TuningWizard.tsx updated with ChartRow insertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-visualizations/06-CONTEXT.md
@.planning/phases/06-visualizations/06-RESEARCH.md
@.planning/phases/06-visualizations/06-UI-SPEC.md
@.planning/phases/06-visualizations/06-01-SUMMARY.md

@creator/src/components/tuning/TuningWizard.tsx
@creator/src/components/tuning/MetricSectionCards.tsx
@creator/src/lib/tuning/chartColors.ts
@creator/src/lib/tuning/chartData.ts
@creator/src/lib/tuning/formulas.ts
@creator/src/lib/tuning/types.ts
@creator/src/types/config.ts

<interfaces>
<!-- Key types and contracts from Plan 01 output. -->

From creator/src/lib/tuning/chartData.ts (created in Plan 01):
```typescript
export interface XpCurvePoint {
  level: number;
  current: number;
  preset: number;
}

export interface MobTierPoint {
  tier: string;
  hp: number;
  damage: number;
  armor: number;
  xp: number;
}

export interface StatRadarPoint {
  stat: string;
  current: number;
  preset: number;
}

export function buildXpCurveData(currentConfig: AppConfig, presetConfig: AppConfig): XpCurvePoint[];
export function buildMobTierData(config: AppConfig, level: number): MobTierPoint[];
export function buildStatRadarData(currentBindings: StatBindings, presetBindings: StatBindings): StatRadarPoint[];
```

From creator/src/lib/tuning/chartColors.ts (created in Plan 01):
```typescript
export const CHART_COLORS = {
  currentSeries: "#aebada",
  presetSeries: "#c8a46a",
  grid: "#39455f",
  axisText: "#95a0bf",
  axisLine: "#56617d",
  barHp: "#c05060",
  barDamage: "#dbb8b8",
  barArmor: "#8caec9",
  barXp: "#a3c48e",
} as const;
```

From creator/src/lib/tuning/types.ts:
```typescript
export interface MetricSnapshot { ... }
```

From creator/src/types/config.ts:
```typescript
export interface AppConfig { ... }
export interface StatBindings { ... }
```

From creator/src/components/tuning/TuningWizard.tsx (insertion context):
- MetricSectionCards renders at ~line 201 inside `{selectedPresetId && currentMetrics && activePresetMetrics && (...)}` guard
- HealthCheckBanner renders at ~line 209
- ChartRow must go BETWEEN MetricSectionCards and HealthCheckBanner
- Available variables: `config` (AppConfig), `presetConfig` (AppConfig | null), `currentMetrics`, `activePresetMetrics`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create XpCurveChart, MobTierChart, StatRadarChart, and ChartRow components</name>
  <files>creator/src/components/tuning/charts/XpCurveChart.tsx, creator/src/components/tuning/charts/MobTierChart.tsx, creator/src/components/tuning/charts/StatRadarChart.tsx, creator/src/components/tuning/charts/ChartRow.tsx</files>
  <read_first>
    creator/src/lib/tuning/chartColors.ts
    creator/src/lib/tuning/chartData.ts
    creator/src/lib/tuning/types.ts
    creator/src/types/config.ts
    creator/src/components/tuning/MetricSectionCards.tsx
    creator/src/components/tuning/TuningWizard.tsx
    .planning/phases/06-visualizations/06-UI-SPEC.md
    .planning/phases/06-visualizations/06-RESEARCH.md
  </read_first>
  <action>
    Create directory `creator/src/components/tuning/charts/` if it does not exist.

    **XpCurveChart.tsx (per D-08, VIZ-01)**
    Create a functional component `XpCurveChart` with props `{ data: XpCurvePoint[] }`:
    - Card wrapper: `<div className="bg-bg-tertiary rounded-lg border border-border-muted p-4">`
    - Title: `<h3 className="font-display text-[14px] font-normal uppercase tracking-[0.5px] text-text-secondary mb-2">XP CURVE</h3>`
    - `<ResponsiveContainer width="100%" height={220}>`
    - `<LineChart data={data} margin={{ top: 5, right: 20, bottom: 5, left: 10 }}>`
    - `<CartesianGrid strokeDasharray="3 3" stroke={CHART_COLORS.grid} />`
    - XAxis: `dataKey="level"`, tick: `{ fill: CHART_COLORS.axisText, fontSize: 12, fontFamily: "'Crimson Pro', Georgia, serif" }`, stroke: `CHART_COLORS.axisLine`
    - YAxis: same tick styling, `tickFormatter={(v: number) => v >= 1000 ? \`${(v / 1000).toFixed(0)}k\` : String(v)}`
    - `<Tooltip />` (Recharts default per D-11)
    - `<Legend />`
    - Current line: `<Line type="monotone" dataKey="current" name="Current" stroke={CHART_COLORS.currentSeries} strokeWidth={2} dot={false} isAnimationActive={true} animationDuration={300} animationEasing="ease-out" />`
    - Preset line: `<Line type="monotone" dataKey="preset" name="Preset" stroke={CHART_COLORS.presetSeries} strokeWidth={2} dot={false} isAnimationActive={true} animationDuration={300} animationEasing="ease-out" />`

    **MobTierChart.tsx (per D-09, VIZ-02)**
    Create a functional component `MobTierChart` with props `{ currentConfig: AppConfig }`:
    - Local state: `const [selectedLevel, setSelectedLevel] = useState(30);` (default Lv30 per D-09)
    - Level options: `[10, 20, 30, 50]`
    - Compute data with useMemo: `const data = useMemo(() => buildMobTierData(currentConfig, selectedLevel), [currentConfig, selectedLevel]);`
    - Card wrapper: `<div className="bg-bg-tertiary rounded-lg border border-border-muted p-4">`
    - Title row: flex container with "MOB POWER" title on left and `<select>` dropdown on right
    - Title: `<h3 className="font-display text-[14px] font-normal uppercase tracking-[0.5px] text-text-secondary">MOB POWER</h3>`
    - Select: `<select className="ornate-input text-[13px] py-0.5 px-2" value={selectedLevel} onChange={(e) => setSelectedLevel(Number(e.target.value))}>`
    - Options: `{[10, 20, 30, 50].map((lv) => <option key={lv} value={lv}>Lv {lv}</option>)}`
    - `<ResponsiveContainer width="100%" height={220}>`
    - `<BarChart data={data} barCategoryGap="20%" margin={{ top: 5, right: 20, bottom: 5, left: 10 }}>`
    - `<CartesianGrid strokeDasharray="3 3" stroke={CHART_COLORS.grid} />`
    - XAxis: `dataKey="tier"`, tick styling same as XpCurveChart
    - YAxis: tick styling same, with `tickFormatter` for large numbers
    - `<Tooltip />`
    - `<Legend />`
    - 4 Bar elements with animation: `<Bar dataKey="hp" name="HP" fill={CHART_COLORS.barHp} isAnimationActive={true} animationDuration={300} animationEasing="ease-out" />`
    - Same pattern for damage (barDamage), armor (barArmor), xp (barXp)

    **StatRadarChart.tsx (per D-10, VIZ-03)**
    Create a functional component `StatRadarChart` with props `{ data: StatRadarPoint[] }`:
    - Card wrapper: `<div className="bg-bg-tertiary rounded-lg border border-border-muted p-4">`
    - Title: `<h3 className="font-display text-[14px] font-normal uppercase tracking-[0.5px] text-text-secondary mb-2">STAT PROFILE</h3>`
    - `<ResponsiveContainer width="100%" height={220}>`
    - `<RadarChart data={data} cx="50%" cy="50%" outerRadius="70%">`
    - `<PolarGrid stroke={CHART_COLORS.grid} />`
    - `<PolarAngleAxis dataKey="stat" tick={{ fill: CHART_COLORS.axisText, fontSize: 11, fontFamily: "'Crimson Pro', Georgia, serif" }} />`
    - `<PolarRadiusAxis tick={false} axisLine={false} />`
    - Current radar: `<Radar name="Current" dataKey="current" stroke={CHART_COLORS.currentSeries} fill={CHART_COLORS.currentSeries} fillOpacity={0.15} strokeWidth={1.5} isAnimationActive={true} animationDuration={300} />`
    - Preset radar: `<Radar name="Preset" dataKey="preset" stroke={CHART_COLORS.presetSeries} fill={CHART_COLORS.presetSeries} fillOpacity={0.25} strokeWidth={1.5} isAnimationActive={true} animationDuration={300} />`
    - `<Tooltip />`
    - `<Legend />`

    **ChartRow.tsx (per D-02, D-03, D-04)**
    Create a container component `ChartRow` with props:
    ```typescript
    interface ChartRowProps {
      currentConfig: AppConfig;
      presetConfig: AppConfig;
      currentMetrics: MetricSnapshot;
      presetMetrics: MetricSnapshot;
    }
    ```
    - Compute data with useMemo:
      - `xpCurveData = useMemo(() => buildXpCurveData(currentConfig, presetConfig), [currentConfig, presetConfig])`
      - `statRadarData = useMemo(() => buildStatRadarData(currentConfig.stats.bindings, presetConfig.stats.bindings), [currentConfig, presetConfig])`
    - Layout: `<div className="animate-unfurl-in grid grid-cols-3 gap-4 px-6 mt-4 mb-4">`
    - Render: `<XpCurveChart data={xpCurveData} />`, `<MobTierChart currentConfig={presetConfig} />`, `<StatRadarChart data={statRadarData} />`
    - Note: MobTierChart receives `presetConfig` as its `currentConfig` prop because it shows the preset's mob tier data at the selected level (the builder is evaluating the preset, per UI-SPEC).

    All components use named exports (no default exports). All imports use the `@/` path alias.
  </action>
  <verify>
    <automated>cd creator && bunx tsc --noEmit && bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export function XpCurveChart" creator/src/components/tuning/charts/XpCurveChart.tsx` returns a match
    - `grep "export function MobTierChart" creator/src/components/tuning/charts/MobTierChart.tsx` returns a match
    - `grep "export function StatRadarChart" creator/src/components/tuning/charts/StatRadarChart.tsx` returns a match
    - `grep "export function ChartRow" creator/src/components/tuning/charts/ChartRow.tsx` returns a match
    - `grep "LineChart" creator/src/components/tuning/charts/XpCurveChart.tsx` returns a match
    - `grep "BarChart" creator/src/components/tuning/charts/MobTierChart.tsx` returns a match
    - `grep "RadarChart" creator/src/components/tuning/charts/StatRadarChart.tsx` returns a match
    - `grep "ResponsiveContainer" creator/src/components/tuning/charts/XpCurveChart.tsx` returns a match
    - `grep "grid-cols-3" creator/src/components/tuning/charts/ChartRow.tsx` returns a match
    - `grep "animate-unfurl-in" creator/src/components/tuning/charts/ChartRow.tsx` returns a match
    - `grep "animationDuration={300}" creator/src/components/tuning/charts/XpCurveChart.tsx` returns a match
    - `grep "useState.*30" creator/src/components/tuning/charts/MobTierChart.tsx` returns a match
    - `grep "ornate-input" creator/src/components/tuning/charts/MobTierChart.tsx` returns a match
    - `grep "XP CURVE" creator/src/components/tuning/charts/XpCurveChart.tsx` returns a match
    - `grep "MOB POWER" creator/src/components/tuning/charts/MobTierChart.tsx` returns a match
    - `grep "STAT PROFILE" creator/src/components/tuning/charts/StatRadarChart.tsx` returns a match
    - `grep "fillOpacity={0.15}" creator/src/components/tuning/charts/StatRadarChart.tsx` returns a match (current radar)
    - `grep "fillOpacity={0.25}" creator/src/components/tuning/charts/StatRadarChart.tsx` returns a match (preset radar)
    - `bunx tsc --noEmit` exits 0
    - `bun run test` exits 0
  </acceptance_criteria>
  <done>
    Four chart component files created: XpCurveChart (LineChart), MobTierChart (BarChart with level dropdown), StatRadarChart (RadarChart), and ChartRow (grid-cols-3 container). All use CHART_COLORS constants, Recharts ResponsiveContainer, 300ms ease-out animations, and Arcanum typography. Type check and test suite pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire ChartRow into TuningWizard between MetricSectionCards and HealthCheckBanner</name>
  <files>creator/src/components/tuning/TuningWizard.tsx</files>
  <read_first>
    creator/src/components/tuning/TuningWizard.tsx
    creator/src/components/tuning/charts/ChartRow.tsx
  </read_first>
  <action>
    Edit `creator/src/components/tuning/TuningWizard.tsx`:

    1. Add import at top with other component imports:
    ```typescript
    import { ChartRow } from "./charts/ChartRow";
    ```

    2. Insert ChartRow between MetricSectionCards and HealthCheckBanner (per D-02). The current code has:
    ```tsx
    {/* Metric summary cards (D-05, D-06) */}
    {selectedPresetId && currentMetrics && activePresetMetrics && (
      <MetricSectionCards ... />
    )}

    {/* Health check banner (D-08) -- after metric cards, before search */}
    <HealthCheckBanner />
    ```

    Insert this block BETWEEN MetricSectionCards and HealthCheckBanner:
    ```tsx
    {/* Chart visualizations (VIZ-01, VIZ-02, VIZ-03) */}
    {selectedPresetId && currentMetrics && activePresetMetrics && presetConfig && (
      <ChartRow
        currentConfig={config}
        presetConfig={presetConfig}
        currentMetrics={currentMetrics}
        presetMetrics={activePresetMetrics}
      />
    )}
    ```

    The guard condition checks `presetConfig` (non-null when a preset is selected and config exists) -- this matches the conditional render pattern from D-04. ChartRow receives:
    - `currentConfig={config}` -- the user's current AppConfig
    - `presetConfig={presetConfig}` -- the deep-merged AppConfig with preset overlay
    - `currentMetrics` and `presetMetrics` -- the MetricSnapshot objects (passed through to ChartRow for future use even though current charts compute data from config directly)

    3. Verify the final render order is:
    - Preset card row
    - MetricSectionCards (conditional)
    - ChartRow (conditional)
    - HealthCheckBanner
    - SearchFilterBar (sticky)
    - Parameter browser
    - ApplyFooterBar (conditional)
  </action>
  <verify>
    <automated>cd creator && bunx tsc --noEmit && bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import.*ChartRow.*from.*charts/ChartRow" creator/src/components/tuning/TuningWizard.tsx` returns a match
    - `grep "ChartRow" creator/src/components/tuning/TuningWizard.tsx` returns at least 2 matches (import + JSX)
    - `grep "currentConfig={config}" creator/src/components/tuning/TuningWizard.tsx` returns a match
    - `grep "presetConfig={presetConfig}" creator/src/components/tuning/TuningWizard.tsx` returns a match
    - `bunx tsc --noEmit` exits 0
    - `bun run test` exits 0
  </acceptance_criteria>
  <done>
    TuningWizard.tsx imports ChartRow and renders it between MetricSectionCards and HealthCheckBanner. ChartRow is conditionally rendered only when a preset is selected (matching D-04). Props pass current and preset configs and metrics. Type check and test suite pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of all three charts</name>
  <files>creator/src/components/tuning/TuningWizard.tsx</files>
  <read_first>
    creator/src/components/tuning/TuningWizard.tsx
  </read_first>
  <action>
    This is a human verification checkpoint. No code changes needed. The executor should launch the app and present the verification steps to the user.

    Run `cd creator && bun run tauri dev` to launch the application in development mode.
  </action>
  <what-built>Three Recharts-based chart visualizations in the Tuning Wizard: XP Curve line chart (levels 1-50, current vs preset), Mob Power grouped bar chart (HP/damage/armor/XP with level selector), and Stat Profile radar chart (8 stat dimensions, current vs preset). Charts appear in a 3-column grid between metric summary cards and the health check banner.</what-built>
  <how-to-verify>
    1. Run `cd creator && bun run tauri dev` to launch the app
    2. Open a project with an application.yaml
    3. Navigate to the Tuning Wizard tab in the sidebar
    4. Verify NO charts appear before selecting a preset
    5. Click the "Casual Adventure" preset card
    6. Verify three chart cards appear in a horizontal row below the metric summary cards:
       - Left: "XP CURVE" -- two lines (muted for Current, gold for Preset) across levels 1-50
       - Center: "MOB POWER" -- grouped bars for Weak/Standard/Elite/Boss tiers, with a level dropdown defaulting to "Lv 30"
       - Right: "STAT PROFILE" -- two overlapping radar polygons (muted = Current, gold = Preset)
    7. Change the mob power level dropdown from "Lv 30" to "Lv 10" -- bars should animate to new heights
    8. Switch to "Hardcore" preset -- all three charts should animate smoothly to new values (300ms transitions)
    9. Click the selected preset card again to deselect -- charts should disappear
    10. Verify chart titles use Cinzel font (uppercase, display style)
    11. Verify axis labels use Crimson Pro font
    12. Verify the warm gold preset line/polygon visually dominates over the muted current line/polygon
    13. Hover over data points -- default Recharts tooltips should show exact values
  </how-to-verify>
  <verify>
    <automated>cd creator && bunx tsc --noEmit && bun run test</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms XP curve chart shows two lines (current vs preset) across 50 levels
    - Human confirms mob tier chart shows grouped bars with working level dropdown
    - Human confirms stat radar chart shows two overlapping polygons
    - Human confirms charts appear only when preset is selected
    - Human confirms chart animations are smooth on preset switch
    - Human confirms Arcanum design system colors and fonts are used
  </acceptance_criteria>
  <done>
    User has visually verified all three chart visualizations render correctly with proper styling, interactivity, and animations. Charts follow the Arcanum design system and match the UI-SPEC layout contract.
  </done>
  <resume-signal>Type "approved" or describe any visual issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | No new trust boundaries -- all data is locally computed from in-memory AppConfig, rendered as SVG. No user input sent externally. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Spoofing | Chart components | accept | Charts render from in-memory config data only. No external data sources. No authentication context. |
| T-06-04 | Denial of Service | buildXpCurveData loop | accept | Loop iterates exactly 50 times (levels 1-50). buildMobTierData iterates 4 tiers. buildStatRadarData iterates 8 stats. All bounded, no user-controlled iteration count. |
</threat_model>

<verification>
- `cd creator && bunx tsc --noEmit` passes
- `cd creator && bun run test` passes (full suite)
- ChartRow renders conditionally only when preset is selected
- Three chart cards visible in grid-cols-3 layout
- XP curve shows dual lines for current vs preset
- Mob tier shows grouped bars with working level selector
- Stat radar shows dual polygons with correct fill opacity
- Charts animate on preset switch (300ms)
</verification>

<success_criteria>
All three chart visualizations render correctly in the Tuning Wizard. Charts appear only when a preset is selected. XP curve compares levels 1-50 for current vs preset. Mob tier shows grouped bars at a selectable level. Stat radar compares 8 stat dimensions. All use Arcanum design system colors and fonts. Charts animate smoothly on data changes. Human verification confirms visual correctness.
</success_criteria>

<output>
After completion, create `.planning/phases/06-visualizations/06-02-SUMMARY.md`
</output>
